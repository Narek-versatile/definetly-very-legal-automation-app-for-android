<Window x:Class="AutoFlow.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AutoFlow" Height="660" Width="980"
        WindowStartupLocation="CenterScreen">
  <Grid Margin="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- toolbar -->
    <DockPanel Grid.Row="0" LastChildFill="True" Margin="0,0,0,8">
      <TextBlock Text="Automations:" VerticalAlignment="Center" Margin="0,0,6,0" DockPanel.Dock="Left" />
      <Button Content="Browse…" Click="Browse_Click" DockPanel.Dock="Right" Margin="6,0,0,0" Padding="10,2" />
      <Button Content="Reload" Click="Reload_Click" DockPanel.Dock="Right" Padding="10,2" />
      <TextBox Text="{Binding AutomationsFolder}" IsReadOnly="True" VerticalAlignment="Center" />
    </DockPanel>

    <!-- body -->
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="260" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <!-- left: automations list -->
      <ListBox Grid.Column="0" ItemsSource="{Binding Automations}"
               SelectedItem="{Binding SelectedAutomation}" Margin="0,0,8,0">
        <ListBox.ItemTemplate>
          <DataTemplate>
            <StackPanel Margin="2">
              <TextBlock Text="{Binding Name}" FontWeight="Bold" TextTrimming="CharacterEllipsis" />
              <TextBlock Text="{Binding FileName}" Foreground="Gray" FontSize="11" />
            </StackPanel>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>

      <!-- right -->
      <Grid Grid.Column="1">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="130" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding SelectedAutomation.Description}"
                   TextWrapping="Wrap" Margin="0,0,0,6" Foreground="#444" />

        <GroupBox Grid.Row="1" Header="Variables" Margin="0,0,0,6">
          <DataGrid ItemsSource="{Binding Variables}" AutoGenerateColumns="False" CanUserAddRows="False"
                    HeadersVisibility="Column" MaxHeight="140" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
              <DataGridTextColumn Header="Key" Binding="{Binding Key}" IsReadOnly="True" Width="180" />
              <DataGridTextColumn Header="Value" Width="*"
                                  Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />
            </DataGrid.Columns>
          </DataGrid>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,6">
          <CheckBox Content="Headless" IsChecked="{Binding Headless}" VerticalAlignment="Center" Margin="0,0,14,0" />
          <Button Content="Run" Click="Run_Click" IsEnabled="{Binding CanRun}" Padding="18,4" Margin="0,0,10,0" />
          <TextBlock Text="{Binding ResultText}" Foreground="{Binding ResultBrush}"
                     FontWeight="Bold" VerticalAlignment="Center" />
        </StackPanel>

        <ListView Grid.Row="3" ItemsSource="{Binding Steps}">
          <ListView.View>
            <GridView>
              <GridViewColumn Header="#" Width="32" DisplayMemberBinding="{Binding Number}" />
              <GridViewColumn Header="Step" Width="380" DisplayMemberBinding="{Binding Describe}" />
              <GridViewColumn Header="Status" Width="90">
                <GridViewColumn.CellTemplate>
                  <DataTemplate>
                    <TextBlock Text="{Binding Status}" Foreground="{Binding StatusBrush}" FontWeight="Bold" />
                  </DataTemplate>
                </GridViewColumn.CellTemplate>
              </GridViewColumn>
            </GridView>
          </ListView.View>
        </ListView>

        <TextBox Grid.Row="4" Text="{Binding Log}" IsReadOnly="True" Margin="0,6,0,0"
                 FontFamily="Consolas" TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
      </Grid>
    </Grid>
  </Grid>
</Window>
